@model List<projectWEB.Models.Category>

@if (Model != null && Model.Count > 0)
{
    <div class="banner-section spad">
        <div class="container-fluid">
            <div class="row">
                @foreach (var category in Model)
                {
                    var currentLanguageCategoryRecord = category.CategoryRecords.FirstOrDefault(x => x.LanguageID == 1);

                    <div class="col-lg-4 col-4">
                        <div class="single-banner align-items-center px-3 py-2" title="@("PP.Home.FeaturedCategories.BrowseCategoryProducts".Replace("{category}", currentLanguageCategoryRecord != null ? currentLanguageCategoryRecord.Name : string.Empty))">
                            @Html.Picture(picture: category.Picture, defaultPic: "site/default-bg.jpg", alt: currentLanguageCategoryRecord != null ? currentLanguageCategoryRecord.Name : string.Empty)
                            <a class="inner-text" href="@Url.SearchProducts(category: category.SanitizedName)">
                                <h4>
                                    @(currentLanguageCategoryRecord != null ? currentLanguageCategoryRecord.Name : string.Empty)
                                </h4>
                            </a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
}